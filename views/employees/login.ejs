<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mandilink Employee Login</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    <style>
        :root {
            --soft-blue: #e1e7ff;
            --soft-cyan: #d1f3f9;
            --primary-blue: #2563eb;
            --accent-blue: #60a5fa;
            --white: #fff;
            --card-bg: #fafdff;
            --input-bg: #f2f7fd;
            --border-radius: 20px;
            --design-gradient: linear-gradient(120deg, #e1e7ff 0%, #d1f3f9 100%);
        }
        html, body {
            height: 100%;
            min-height: 100%;
        }
        body {
            font-family: 'Arial', sans-serif;
            background: var(--design-gradient);
            min-height: 100vh;
            margin: 0;
            padding: 0;
            height: 100vh;
            width: 100vw;
        }
        .login-wrapper {
            display: flex;
            width: 100vw;
            min-height: 100vh;
            height: 100vh;
        }
        .login-left {
            flex: 1 1 50%;
            background: var(--design-gradient);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 60px 30px;
            position: relative;
            overflow: hidden;
        }
        .design-title {
            color: var(--primary-blue);
            font-size: 2rem;
            font-weight: bold;
            margin-bottom: 0.7em;
            letter-spacing: 1px;
            z-index: 2;
            position: relative;
        }
        .design-desc {
            color: #4c5d80;
            font-size: 1.18rem;
            text-align: center;
            font-weight: 500;
            margin-bottom: 2em;
            z-index: 2;
            position: relative;
        }
        /* Floating illustration styles */
        .floating-illustration {
            position: absolute;
            left: 50%;
            transform: translateX(-50%);
            z-index: 1;
            width: 80%;
            max-width: 380px;
            opacity: 0.97;
            animation: float-y 6s ease-in-out infinite alternate;
            filter: drop-shadow(0 12px 40px rgba(96,165,250,0.11));
            bottom: 0;
        }
        @keyframes float-y {
            0% { transform: translateX(-50%) translateY(0px) scale(1); }
            100% { transform: translateX(-50%) translateY(-16px) scale(1.035); }
        }
        /* Additional floating geometries for futuristic effect */
        .circle1, .circle2, .circle3 {
            position: absolute;
            border-radius: 50%;
            opacity: 0.17;
            z-index: 0;
            animation: float-circles 8s ease-in-out infinite alternate;
        }
        .circle1 {
            width: 170px; height: 170px;
            background: var(--primary-blue);
            left: 18%; top: 5%;
            animation-delay: 0s;
        }
        .circle2 {
            width: 80px; height: 80px;
            background: var(--accent-blue);
            left: 70%; top: 18%;
            animation-delay: 2s;
        }
        .circle3 {
            width: 120px; height: 120px;
            background: #fcfbff;
            left: 65%; top: 70%;
            animation-delay: 1.5s;
        }
        @keyframes float-circles {
            0% { transform: translateY(0); }
            100% { transform: translateY(-28px) scale(1.08);}
        }
        .login-right {
            flex: 1 1 50%;
            background: var(--card-bg);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 44px 36px;
            position: relative;
        }
        .login-logo {
            width: 62px;
            height: 62px;
            background: darkgreen;
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 16px rgba(96,165,250,0.13);
            margin: 0 auto 18px auto;
        }
        .login-logo img {
            width: 44px;
            height: 44px;
            object-fit: contain;
            border-radius: 10px;
        }
        .login-title {
            color: var(--primary-blue);
            font-size: 1.5rem;
            font-weight: bold;
            margin-bottom: 2px;
        }
        .login-subtitle {
            color: var(--accent-blue);
            margin-bottom: 16px;
            font-size: 1.06rem;
            text-align: center;
            font-weight: 500;
        }
        .form-label {
            font-weight: 500;
            color: #466ab5;
        }
        .form-control {
            border-radius: 12px;
            background: var(--input-bg);
            border: 1px solid var(--accent-blue);
        }
        .form-control:focus {
            box-shadow: 0 0 0 2px var(--accent-blue);
            border-color: var(--primary-blue);
            background: #f0f8ff;
        }
        .input-group-text {
            background: var(--accent-blue);
            border: none;
            color: var(--primary-blue);
            border-radius: 12px 0 0 12px;
        }
        .btn-login {
            margin-top: 10px;
            background: linear-gradient(90deg, var(--accent-blue) 0%, var(--primary-blue) 100%);
            color: #fff;
            border: none;
            border-radius: 12px;
            padding: 12px 0;
            font-weight: bold;
            font-size: 1.07rem;
            width: 100%;
            transition: box-shadow 0.2s, background 0.2s;
            box-shadow: 0 2px 8px rgba(96,165,250,0.11);
            letter-spacing: 1px;
        }
        .btn-login:hover, .btn-login:focus {
            background: linear-gradient(90deg, var(--primary-blue) 10%, var(--accent-blue) 90%);
            color: #fafdff;
            box-shadow: 0 4px 18px rgba(96,165,250,0.17);
        }
        .forgot-link {
            display: block;
            text-align: right;
            margin-top: 8px;
            color: var(--primary-blue);
            text-decoration: none;
            font-size: 0.97rem;
            font-weight: 500;
            transition: color 0.2s;
        }
        .forgot-link:hover {
            color: var(--accent-blue);
            text-decoration: underline;
        }
        .login-footer {
            text-align: center;
            font-size: 0.93rem;
            color: #7ea1c7;
            margin-top: 24px;
        }
        @media (max-width: 900px) {
            .login-wrapper {
                flex-direction: column;
                width: 100vw;
                height: 100vh;
            }
            .login-left, .login-right {
                flex: unset;
                width: 100vw;
                min-height: 0;
                padding: 38px 10vw;
            }
            .login-left {
                border-radius: var(--border-radius) var(--border-radius) 0 0;
                padding-bottom: 24px;
                min-height: 260px;
            }
            .login-right {
                border-radius: 0 0 var(--border-radius) var(--border-radius);
                padding-top: 22px;
            }
        }
        @media (max-width: 600px) {
            .login-wrapper {
                border-radius: 0;
                width: 100vw;
                min-width: 0;
                height: 100vh;
                max-width: 100vw;
            }
            .login-left, .login-right {
                padding: 22px 5vw;
            }
            .design-title { font-size: 1.2rem; }
            .design-desc { font-size: 0.98rem; }
            .login-title { font-size: 1.1rem; }
        }
    </style>
</head>
<body>
    <div class="login-wrapper">
        <!-- Left design column with floating future illustration and geometries -->
        <div class="login-left">
            <div class="design-title">Welcome to Mandilink</div>
            <div class="design-desc">
                The future of work, today.<br>
                Manage your tasks, connect, and grow in a smart workplace.
            </div>
            <!-- Futuristic floating SVG illustration (undraw.co, etc), here is an example. Replace with your own if you prefer. -->
            <img class="floating-illustration" src="/illustration/employee.png" alt="Futuristic Illustration">
            <div class="circle1"></div>
            <div class="circle2"></div>
            <div class="circle3"></div>
        </div>
        <!-- Right form column -->
        <div class="login-right">
            <div class="login-logo">
                <img src="/logo/logo.png" alt="Mandilink Logo">
            </div>
            <div class="login-title">Employee Login</div>
            <div class="login-subtitle">Sign in to access your Mandilink dashboard</div>
            <form style="width:100%;max-width:340px;" action="/employees/auth/login" method="POST">
                <div class="mb-3">
                    <label for="employeeEmail" class="form-label">Email address</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="fas fa-envelope"></i></span>
                        <input type="email" class="form-control" id="employeeEmail" placeholder="employee@example.com" name="email" required>
                    </div>
                </div>
                <div class="mb-3">
                    <label for="employeePassword" class="form-label">Password</label>
                    <div class="input-group">
                        <span class="input-group-text"><i class="fas fa-lock"></i></span>
                        <input type="password" class="form-control" id="employeePassword" placeholder="Password" name="password" required>
                    </div>
                </div>
                <a class="forgot-link" href="#">Forgot password?</a>
                <button type="submit" class="btn btn-login">Login</button>
            </form>
            <div class="login-footer">
                &copy; 2025 Mandilink. All Rights Reserved.
            </div>
        </div>
    </div>
</body>
</html>